from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Fine-tuning transformer-based models for generating trade recommendations.',
    author='Loc Tran',
    license='',
)
